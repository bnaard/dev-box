// Generated by aibox — do not edit manually.
// To modify, edit aibox.toml and run: aibox apply
{
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "rust-lang.rust-analyzer",
        "dbaeumer.vscode-eslint"
      ],
      "settings": {
        "terminal.integrated.defaultProfile.linux": "bash",
        "terminal.integrated.fontFamily": "'FiraCode Nerd Font', 'Hack Nerd Font', 'JetBrainsMono Nerd Font', monospace",
        "terminal.integrated.profiles.linux": {
          "bash": {
            "path": "/bin/bash"
          },
          "codex": {
            "path": "/usr/local/bin/codex"
          },
          "zellij": {
            "args": [
              "--layout",
              "ai"
            ],
            "path": "/usr/local/bin/zellij"
          }
        }
      }
    }
  },
  "dockerComposeFile": [
    "docker-compose.yml",
    "docker-compose.override.yml"
  ],
  "name": "aibox",
  "overrideCommand": false,
  "portsAttributes": {
    "4714": {
      "onAutoForward": "ignore"
    }
  },
  "postStartCommand": "if ! pgrep -f 'processkit-gateway/mcp/server.py.*streamable-http' >/dev/null 2>&1; then mkdir -p /tmp/aibox/uv-cache; (cd /workspace && UV_CACHE_DIR=/tmp/aibox/uv-cache PROCESSKIT_MCP_MODE=gateway nohup uv run context/skills/processkit/processkit-gateway/mcp/server.py serve --transport streamable-http --host 127.0.0.1 --port 8765 >/tmp/aibox/processkit-gateway.log 2>&1 &); fi",
  "remoteUser": "aibox",
  "service": "aibox",
  "workspaceFolder": "/workspace"
}
