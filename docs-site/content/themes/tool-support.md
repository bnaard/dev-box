---
title: Tool support
linkTitle: Tool support
description: See how each supported terminal tool consumes the shared semantic theme.
weight: 30
themeView: tool-support
---

The semantic palette is shared, while each tool receives only the colors and
emphasis that its own configuration format supports.
