---
apiVersion: processkit.projectious.work/v2
kind: WorkItem
metadata:
  id: BACK-20260508_1517-SnowyWillow-doctor-coverage-gap-closure-work
  created: '2026-05-08T15:17:42+00:00'
  labels:
    track: doctor-gaps
    release: v0.25.6
  updated: '2026-05-08T19:56:24+00:00'
spec:
  title: 'v0.25.6: Doctor coverage gap closure'
  state: done
  type: task
  priority: high
  description: |
    ## Goal
    Add the seven missing checks identified in the v0.25.6 review so each reported user symptom would have surfaced as an actionable warning/error before manifesting.

    ## Checks to add (file: `cli/src/doctor.rs` unless noted)

    1. **Legacy `mode = "powerline"` toml lint** — after config load, scan raw toml for `[customization.tmux.status] mode = "powerline"` (and the legacy `[tmux.status]` form). Warn with one-line rewrite suggestion: `change to mode = "extended" (powerline is a deprecated alias)`.

    2. **Disabled-tool image-binary absence check** — for every addon tool with `enabled=false`, inspect generated `.devcontainer/Dockerfile` and (when available) probe the running container for the binary's presence. Warn if found.

    3. **PowerKit plugin tree presence** — when `mode == extended`, assert `.aibox-home/.tmux/plugins/tmux-powerkit/` exists OR the in-image `/usr/local/share/aibox/tmux/plugins/tmux-powerkit/tmux-powerkit.tmux` is present.

    4. **Tmux.conf corruption signature** — scan live `.aibox-home/.config/tmux/tmux.conf` for `set -g status off` combined with `set -g status-right " off_RIGHT "`; report as the v0.25.3 substitution-order corruption with remediation `aibox apply` (after lockfile bump 0.25.6+).

    5. **Lockfile vs current host CLI** — read `aibox.lock.cli_version` and compare against `env!("CARGO_PKG_VERSION")`; warn loudly if the host CLI is newer than the lock and `aibox apply` has not been run yet (the most common "why am I still hitting the old bug" cause).

    6. **Tmux managed socket pre-validation** — before any `aibox up` attach attempt, verify `$AIBOX_TMUX_SOCKET` env or `$HOME/.tmux/aibox.sock` parent dir exists. Wire this into `cmd_start` not just doctor.

    7. **Yazi terminal-response startup probe** — when running inside the container, run `timeout 4s yazi --debug` against `/dev/null` and grep for `Terminal response timeout`; warn with reference to fixed `tool_or_shell` wait-loop (regenerated by apply).

    8. **Zellij artifact presence (loud error)** — see BR-ZELLIJ-EXCISE; this check lives here.

    ## Acceptance criteria
    - Every check has an e2e test in `cli/tests/e2e/doctor.rs` exercising both the trigger condition and the clean state.
    - `aibox doctor --json` (if exists; else add) outputs structured codes for each warning so CI can parse them.
    - Doctor end-of-run summary includes counts per category.

    ## Dispatch hint for next session
    One general-purpose subagent. Items 1-5 are pure config/file scans (fast). Items 6-7 require runtime probing — gate behind `--probe-runtime` flag if expensive. Sequential.
  started_at: '2026-05-08T19:56:06+00:00'
  completed_at: '2026-05-08T19:56:24+00:00'
---

## Transition note (2026-05-08T19:56:06+00:00)

Implementation landed in commit 27d1510 — six new health checks + semver-aware version skew.


## Transition note (2026-05-08T19:56:18+00:00)

Code shipped via 27d1510; ready for accept-as-done.


## Transition note (2026-05-08T19:56:24+00:00)

Accepted as done — six new health checks + semver-aware version skew shipped on main.
